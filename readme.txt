This JavaScript application receives tweets in real time that match a given hashtag.

The main mobile app provides uses a set of data accumulated through the use of this javascript application in order to quiz users on the popularity of the different hashtags.

The last file in the folder is the java fabric integration which would have dynamically altered the popularity of different hashtags.